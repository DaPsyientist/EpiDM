This is an instructional text file that covers the steps necessary in order to use the Pseudorandomization scripts

# Step 1: Install the necessary python packages
In order to do so, you need to run the following command inside your terminal:
"pip install -R requirements.txt"

# Step 2: Move the encoding file which you are interested in into the master folder (The folder that contains the PS1.py script)
# Note: All the files need to be in the same folder as the script to work as intended

# Step 3: Invoke the Python script and pass the necessary files via the command line via the format indicated below:
"python PS1.py ${Encoding_File} OldNew_PS1.csv New_PS1.csv Old_PS1.csv "